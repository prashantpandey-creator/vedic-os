[pytest]
testpaths = tests
# tests/ imports the project modules (core.*, agents.*, config) by top-level name.
# Without this, `pytest` from the repo root fails on ModuleNotFoundError while
# `PYTHONPATH=. pytest` works — a difference nobody should have to know about.
pythonpath = .
